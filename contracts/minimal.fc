int counter = 0;

main(int msg_value, cell in_msg, slice in_msg_body) {
    counter = counter + 1;
}

main_external(slice in_msg_body) {
    // обробка зовнішніх повідомлень
}

get_counter() {
    return counter;
}